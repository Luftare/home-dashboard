{
  "name": "home-dashboard",
  "version": "1.0.0",
  "description": "",
  "main": "server/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prestart": "git pull && cd client && npm install && npm run build && cd ..",
    "start": "node server/index.js",
    "update": "cd client && npm run build && cd .. && git add . && git commit -m 'Add production' && git push",
    "build": "cd client && npm run build",
    "serve": "cd server && node ."
  },
  "author": "LF",
  "license": "ISC"
}
